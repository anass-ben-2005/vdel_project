"""The CSV sales analyzer pipeline the student completes.

This file is intentionally empty of logic. It exists so that `sales_analyzer` is a real
importable package, which is what lets the hidden tests say
`from sales_analyzer.ingest import read_sales_csv` against the student's own repo without
any sys.path manipulation in the test files themselves.

Copied verbatim into every rendered student repo -- it carries no `# @gap:` markers and
is never rendered through gap_parser, because there is nothing in it to complete. Matches
weather_etl/__init__.py's own docstring and role exactly (VDEL_TEN_PROJECT_CURRICULUM.md
§0: "Project 1 is the pattern every other project must match").
"""
