"""The weather ETL pipeline the student completes.

This file is intentionally empty of logic. It exists so that `weather_etl` is a real
importable package, which is what lets the hidden tests say
`from weather_etl.extract import fetch_weather` against the student's own repo without
any sys.path manipulation in the test files themselves.

Copied verbatim into every rendered student repo -- it carries no `# @gap:` markers and
is never rendered through gap_parser, because there is nothing in it to complete.
"""
